import { logHeader } from '../../../styles/gameSession/HistoryLog.module.scss';

export default function LogTitle() {
  return (
    <div className={logHeader}>
      <h1>Session log</h1>
    </div>
  );
};
